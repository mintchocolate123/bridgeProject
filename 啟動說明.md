# 啟動說明

放在專案根目錄,忘記怎麼開的時候看這份。更詳細的說明在各資料夾的 README。

```
_project/
  bridge-core/       裁判程式庫(不用啟動)
  bridge-platform/   平台 + 網站,port 8000
  bridge-ui/         網站原始碼(build 一次就好)
  bridge-bot/        我們的 bot
  bridge-rag/        RAG 服務,port 8001、8002…
```

---

## 第一次安裝(只做一次)

需要 Python 3.10 以上、Node.js 20 以上,用 BEN 的話還要 Docker。

每個 Python 專案都有自己的 venv,互不影響。

```
cd bridge-platform
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements-dev.txt
python -m pytest
deactivate
```

`bridge-bot`、`bridge-rag` 用一樣的指令各做一次(把第一行的資料夾換掉)。
`bridge-core` 不用另外裝,其他專案會自動把它裝進去。

網站:

```
cd bridge-ui
npm install
npm run build
```

改了 `bridge-ui` 的程式才需要再 `npm run build`。

---

## 每次啟動

**順序:平台 → bot 要用的服務 → 四個 bot。**
每個程式開一個終端機,每個終端機都要先 `cd` 到那個資料夾、啟用那個資料夾的 venv。

### 1. 平台

```
cd bridge-platform
.\.venv\Scripts\Activate.ps1
python -m bridge_platform
```

看到 `Uvicorn running on http://127.0.0.1:8000` 就好了。
瀏覽器開 http://localhost:8000 。

要用主辦方頁面,啟動前先在同一個終端機設定金鑰:

```
$env:BRIDGE_ADMIN_KEY = "換成一串夠長的隨機字"
```

要讓別台電腦連進來(例如別隊的 bot),改成 `python -m bridge_platform --host 0.0.0.0`。

### 2. bot 要用的服務(用到才開)

BEN:

```
docker start ben
```

RAG,每個版本一個終端機,各用一個 port:

```
cd bridge-rag
.\.venv\Scripts\Activate.ps1
python rag_service.py --version v0 --port 8001
```

rulebased、passing 不需要任何服務。

### 3. 四個 bot

第一個開房間:

```
cd bridge-bot
.\.venv\Scripts\Activate.ps1
python play.py --agent rulebased --boards 4
```

記下它印出的房號(例如 `K7P2QX`),另外三個用房號加入:

```
python play.py --agent ben --room K7P2QX
python play.py --agent rulebased --room K7P2QX
python play.py --agent rag@http://localhost:8001 --room K7P2QX
```

四家坐滿自動開始。到網站大廳點房間就能觀戰,打完可以看回放。

`--agent` 可以填:

| 寫法 | 意思 |
|---|---|
| `rulebased` | 簡易規則式 |
| `passing` | 全部 pass,測試用 |
| `ben` | BEN(要先 `docker start ben`) |
| `rag@http://localhost:8001` | 接 8001 上的 RAG 版本 |

其他常用參數:`--seat W` 指定座位、`--seed 7` 固定發牌、`--name 名字` 網站上顯示的名稱。

---

## 最簡單的測試

四家都用 `rulebased`:開平台 + 四個 bot,共五個終端機,不需要 BEN 和 RAG。
用來確認整條鏈是通的。

---

## 本機跑實驗(不經過平台)

不用開平台,一個終端機就好:

```
cd bridge-bot
.\.venv\Scripts\Activate.ps1
python main.py --deals 50 --seed 1 --ns rag@http://localhost:8001 --ew ben
```

比較不同設定時,`--seed` 要一樣,才是同一批牌。

---

## 關閉

每個終端機按 `Ctrl+C`。BEN 用 `docker stop ben`。

平台關掉後房間都存在 `bridge-platform/data/`,重新啟動會自動還原;
bot 如果還開著會自動重連,把牌打完。

---

## 常見問題

| 狀況 | 原因與解法 |
|---|---|
| `Activate.ps1` 無法執行,說停用了指令碼 | PowerShell 預設不讓跑腳本。執行一次 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` |
| `ModuleNotFoundError: bridge_core` | venv 沒啟用,或 requirements 沒重裝。啟用後再 `python -m pip install -r requirements-dev.txt` |
| 網站打開是 404 或空白 | 還沒 build。到 `bridge-ui` 執行 `npm run build`,再重開平台 |
| `address already in use` / port 被佔用 | 那個 port 已經有程式在跑,多半是上次沒關掉。關掉它或換 port |
| bot 說連不上平台 | 平台沒開,或 `--server` 網址不對 |
| 用 BEN 第一手很慢 | 正常,模型第一次要載入 10~40 秒,bot 入座前會先暖機 |
| 終端機出現「備援」 | agent 出錯、回了不合法的動作或想太久,這一手改用規則式代打。紀錄裡會標記 `fallback_used` |
| 房間被終止,原因是逾時 | 某個 bot 連續兩回合超過 300 秒沒動作,多半是那個 bot 的程式已經關掉或當掉 |
| 平台用 `uvicorn` 啟動後關不掉 | 請用 `python -m bridge_platform` 啟動,它關閉時最多等 3 秒 |
